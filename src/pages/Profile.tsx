import { Card } from '../components/ui'
export default function Profile() {
  return (
    <Card>
      <h1 className="text-xl font-semibold">Profile</h1>
      <p className="mt-2 text-sm text-muted">Your profile — coming in Phase 1.</p>
    </Card>
  )
}
